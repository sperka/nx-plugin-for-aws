RecipeFormatVersion: '<%- recipeFormat %>'
ComponentName: <%- componentName %>
ComponentVersion: <%- version %>
ComponentDescription: <%- componentDescription %>
ComponentPublisher: <%- publisher %>
<%_ if (ipc) { _%>
ComponentConfiguration:
  DefaultConfiguration:
    accessControl:
      aws.greengrass.ipc.pubsub:
        <%- componentName %>:pubsub:1:
          policyDescription: Allow publishing heartbeat messages to this component's own topic.
          operations:
            - aws.greengrass#PublishToTopic
          resources:
            - <%- componentName %>/heartbeat
<%_ } _%>
Manifests:
  - Platform:
      os: <%- manifestOs %>
      architecture: <%- manifestArchitecture %>
    Artifacts:
      # Placeholders are substituted at publish time. The basename must stay
      # <%- componentDirName %>.zip, which is what the Run script below unarchives to.
      - Uri: s3://BUCKET_NAME/COMPONENT_NAME/COMPONENT_VERSION/<%- componentDirName %>.zip
        Unarchive: ZIP
    Lifecycle:
      Run: python3 {artifacts:decompressedPath}/<%- componentDirName %>/main.py
