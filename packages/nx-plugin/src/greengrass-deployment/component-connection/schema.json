{
  "$schema": "https://json-schema.org/schema",
  "$id": "greengrass-deployment#component-connection",
  "title": "greengrass-deployment#component-connection",
  "description": "Connect a Greengrass component to a greengrass-deployment project, adding it to the deployment's component map",
  "type": "object",
  "properties": {
    "sourceProject": {
      "type": "string",
      "description": "The greengrass-deployment project"
    },
    "targetProject": {
      "type": "string",
      "description": "The project hosting the Greengrass component"
    },
    "sourceComponent": {
      "type": "string",
      "description": "Unused - a greengrass-deployment is always the connection's source project, never one of its components"
    },
    "targetComponent": {
      "type": "string",
      "description": "The Greengrass component in the target project"
    },
    "preferInstallDependencies": {
      "type": "boolean",
      "description": "Whether to prefer installing dependencies after the generator runs. Set to false to defer installing when batching multiple generators (an install still runs if needed so subsequent generators can compute the Nx project graph); install once at the end.",
      "default": true
    }
  },
  "required": ["sourceProject", "targetProject"]
}
